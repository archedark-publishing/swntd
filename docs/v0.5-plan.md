# SWNTD v0.5

Implement and validate each item in order on `codex/v0.5`. Keep the PR in draft; staging data may be changed for verification. Record unresolved product choices here for Josh.

- [x] Add favicon.
- [x] Preserve line breaks and formatting in comments.
- [x] Add history to task detail.
- [x] Tune count-per-period calculations in retrospectives.
- [x] Refine commitment formatting, especially on mobile.
- [x] Allow Ctrl+Enter to submit desktop comments.
- [x] Add label filters to the board.
- [x] Allow comment editing.
- [x] Make browser Back close task detail before leaving the site.
- [x] Add photo and attachment previews.

## Validation

Run focused checks after each item, then full lint, typecheck, tests, and build. Verify desktop and mobile UI and deploy the draft PR to staging.

## Decisions and questions

Use existing product and permission conventions. Record assumptions and any remaining questions as implementation proceeds.

Count decisions: intervals stay anchored to the period start. Monthly boundaries clamp to the month end without drift. The inclusive final interval gets a prorated target rounded up to a whole count. Extra progress remains loggable.

Label filters match any selected label and combine with Only My Tasks.

## Completed validation

- Full `pnpm lint`, `pnpm typecheck`, `pnpm test` (63 tests), and `pnpm build` pass.
- Browser checks on local desktop and 390px mobile: Markdown, Ctrl+Enter posting, inline editing, task history, Back/Forward, label filters plus Only My Tasks, image/PDF previews, and count progress beyond the target.
- Local fixtures use a separate temporary database and upload directory.
- Staging comment creation and editing passed; the draft PR deployed through the manual workflow because automatic deployment skips draft PRs.
- The extension cannot select local upload files without file-URL access. Local upload API and browser previews passed. No browser settings were changed.

## Review notes

- Author-only comment edits use timestamps to reject stale writes; history records edits without exposing raw event payloads.
- Previews support browser-readable images, PDF pages, and audio. Other formats retain original downloads; HEIC support depends on the browser.
- PDF rendering loads only when a PDF is opened. Vite still reports its existing main-bundle size advisory.
- No database migration or production deployment is needed for this release. Keep the PR in draft for Josh's review.

Final staging deployment succeeded (workflow run 33946259635), with checkout verified at implementation commit `5b5d961`. Staging health, favicon, comment formatting/editing/history, and label filtering passed. Browser Back also preserved pending local task edits.
